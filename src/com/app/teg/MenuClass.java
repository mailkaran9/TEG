package com.app.teg;
/*package com.app.teg;

import android.app.Activity;
import android.os.Bundle;
import android.view.Menu;
import android.view.MenuInflater;
import android.view.MenuItem;
import android.widget.Toast;

public class MenuClass extends Activity {
    @Override
    public void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.homescreen);
    }
    
    /* Initiating Menu XML file (menu.xml) */
 /*   @Override
    public boolean onCreateOptionsMenu(Menu menu)
    {
        MenuInflater menuInflater = getMenuInflater();
        menuInflater.inflate(R.menu.cool_menu, menu);
        return true;
    }
    
    /**
     * Event Handling for Individual menu item selected
     * Identify single menu item by it's id
     * 
     *  * */
  /*  @Override
    public boolean onOptionsItemSelected(MenuItem item)
    {
        
        switch (item.getItemId())
        {
        case R.id.home:
        	// Single menu item is selected do something
        	// Ex: launching new activity/screen or show alert message
            Toast.makeText(MenuClass.this, "Home is Selected", Toast.LENGTH_SHORT).show();
            return true;
        case R.id.addEvent:
        	Toast.makeText(MenuClass.this, "Save is Selected", Toast.LENGTH_SHORT).show();
            return true;
        case R.id.societies:
        	Toast.makeText(MenuClass.this, "Search is Selected", Toast.LENGTH_SHORT).show();
            return true;
      
        case R.id.developers:
        	Toast.makeText(MenuClass.this, "Delete is Selected", Toast.LENGTH_SHORT).show();
            return true;
       
        default:
            return super.onOptionsItemSelected(item);
        }
    }
     
    

}*/